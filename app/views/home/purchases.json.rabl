# collection @purchases.to_a, root: false, object_root: false

# attributes :id, :platform, :price, :version, :modified

collection @hours.to_a, root: false, object_root: false

attributes :hour, :platform, :price, :restored, :count